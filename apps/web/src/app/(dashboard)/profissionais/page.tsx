export default function ProfissionaisPage() {
  return (
    <div>
      <h2 className="text-xl font-semibold text-gray-900">Profissionais</h2>
      <p className="mt-2 text-sm text-gray-500">
        Tela em desenvolvimento por Nicolas (Front-end). A API já está pronta em{' '}
        <code className="rounded bg-gray-100 px-1">GET/POST /api/profissionais</code>.
      </p>
    </div>
  );
}
