services:
  api:
    command: npm run start:dev
    volumes:
      - ./apps/api:/app
      - /app/node_modules

  web:
    command: npm run dev
    volumes:
      - ./apps/web:/app
      - /app/node_modules
